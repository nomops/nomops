# Product

## Register

product

## Users

自托管的开发者、自动化工程师、平台/集成团队（DevOps、内部工具、系统集成）；Cloud 形态另面向多租户运营方。

- **上下文**：在自己的基础设施上搭建、调试、运行工作流。信息密度高、长时停留的专业工具场景，而非一次性浏览。
- **要完成的事（job-to-be-done）**：把「触发 → 取数 → 变换 → 落库 / 通知」这类集成自动化，用可视化画布搭出来，必要时下钻到代码（Code 节点 + 表达式），并能看到每一步节点的输入 / 输出、可靠地跑起来、出错能复查重跑。

## Product Purpose

nomops 是节点式可视化工作流自动化平台，采用「共享内核 + 双部署形态」：先交付可自部署的**安装版（self-hosted，本仓库）**，再在其外包一层多租户 **Cloud**（独立仓库 `nomops-cloud`，零代码耦合）。

产品形态是对一款成熟**基线产品**的**高保真复刻**——信息架构、交互行为、视觉全部对齐基线，但跑在自有的三层解耦引擎（`workflow` / `core` / `nodes`）与 Vue 3 + Vue Flow 画布上，视觉由自有 `design-tokens.css`（从基线逐字提取、883/883 计算值等值验收）驱动。

成功的样子：① 基线已覆盖的界面达到**像素级一致**（diff-ledger 清零）；② 自有增量功能（agents 平台 / AI builder / 动态凭证 / 联邦信任等 feature-backlog 项）在**同一套设计语言**下自然延伸，看不出「拼接感」。

## Brand Personality

- **声音 / 语气**：克制、专业、工具本位。面向工程师，信息密度高，少营销腔、少形容词。英文 UI。
- **三词个性**：precise（精确）· dense（信息密集）· utilitarian（实用主义）。
- **情绪目标**：**掌控感**（confidence）与**一眼看清每一步数据流**（legibility）——不是 delight，不是华丽。
- **视觉基调**：深色为默认主题（`data-theme="dark"`），单一橙 `#ff6900` 作唯一强调色，其余为中性灰阶；4px 圆角；InterVariable（正文）+ CommitMono（代码 / 表达式）。

## Anti-references

复刻项目的「反面」很具体，务必避开：

- **对基线已覆盖界面「重新设计」的一切冲动**——换布局、改配色、加装饰、加动效、加卡片，都是**缺陷（diff-ledger 条目）而非改进**。
- **通用 SaaS 模板腔**：等大卡片网格堆砌、hero-metric 大数字模板、渐变文字（`background-clip:text`）、玻璃拟态装饰、每段小标题上方的 uppercase tracked eyebrow、`01/02/03` 编号章节——这些都不属于基线的工具语言。
- **裸色值 / 脱离 `design-tokens.css` 的一次性样式**（真值唯一化，禁止硬编码颜色）。
- **复制第三方厂商 logo**（商标风险）——用品牌色字母 monogram 芯片代替。
- **浅色优先的假设**——本产品深色为默认。
- **明文凭证**出现在任何 UI / 日志 / API 响应中。
- 在仓库内出现基线产品的**真实名称**——内部一律称「基线」。

## Design Principles

1. **基线即规格（Fidelity is the spec）**——对基线已覆盖的界面，一致性压倒创意。任何偏差先进 `diff-ledger.md`，用**双侧截图 + live computed-style 数值比对**验收，不靠肉眼「差不多」。P0=结构缺失 / P1=一眼可见错误 / P2=数值级偏差。
2. **令牌唯一真值（One token source of truth）**——所有视觉走 `design-tokens.css` 真名，禁止裸色值与一次性样式；换主题、调品牌色只动令牌层，存量组件零改动继承。
3. **延伸而非另起炉灶（Extend, don't reinvent）**——自有新功能先找**最近的基线模式**去延伸，复用其组件、布局语法与令牌，让新界面「像是基线原生的」。greenfield 冲动留给引擎，不留给 UI。
4. **每一步数据可见（Show the data at every step）**——产品核心价值是节点输入 / 输出的可观测性。界面为「看清数据流」服务，装饰性一律让位于可读性与信息密度。
5. **工具本位的克制（Utilitarian restraint）**——信息密度与可操作性优先；深色默认、单一橙强调、动效最小化。掌控感是目标，delight 不是。

## Accessibility & Inclusion

**与基线对齐（parity），不设独立更高门槛。** 复刻的可达性行为跟随基线：对比度、焦点表现、`prefers-reduced-motion` 等复用基线的实现，而非另立标准——因为「为改善可达性而偏离基线视觉」属于 diff-ledger 的讨论范畴，不是默认动作。

- 深色为默认主题，橙 `#ff6900` 为唯一强调色；涉及仅靠颜色传达状态时，跟随基线的图标 / 文案冗余做法。
- 自有增量界面沿用基线的对比度与键盘 / 焦点表现，保持体感一致。
